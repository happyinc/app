<?php 
require_once'../../externo/plugins/PDOModel.php';

$producto = $_POST['id_producto']; 
$con = new PDOModel();
$con->where("id_producto", $producto);//setting where condition
$con->delete("composicion_producto");

echo "se acualizo la lista de las composiciones";
?>